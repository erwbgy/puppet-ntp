0.7.3:

* Use Ruby instance variables in templates (thanks #seb-github)

* Support Amazon Linux (thanks #antonbabenko)

0.7.2:

* Don't attempt to install the ntpdate package if the operating system version
is < 6.0 as it doesn't exist (#1 thanks dustyketchum).

0.7.1:

* Regression: restart the NTP service if the config changes

0.7.0:

* Use augeas to change server entries leaving the rest of the config file
alone

0.6.1:

* Added support for the iburst parameter (thanks Greg Anderson)

